# Bissultan Kuandykov - Day 29 Flutter

## 📋 Задание: Локальная база данных - Drift и SQLite

### ✅ Выполненные задания

1. **Создать 2 таблицы с Foreign Key**
   - ✅ Таблица `Tasks` (задачи)
   - ✅ Таблица `Tags` (теги)
   - ✅ Foreign Key связь: `Tasks.tagId → Tags.id`
   - ✅ onDelete: KeyAction.setNull

2. **CRUD операции + Сортировка**
   - ✅ Create - создание задач и тегов
   - ✅ Read - чтение с JOIN запросами
   - ✅ Update - обновление с отслеживанием времени
   - ✅ Delete - удаление записей
   - ✅ Сортировка по дате (DESC)
   - ✅ Сортировка по приоритету (DESC)

3. **Stream (watch) vs Get - наглядное сравнение**
   - ✅ Tab 1: Stream - автоматическое обновление UI
   - ✅ Tab 2: Get - ручное обновление (требует кнопки 🔄)
   - ✅ Tab 3: Сортировка с реактивностью

---

## 🚀 Запуск проекта

### Шаг 1: Установить зависимости
```bash
flutter pub get
```

### Шаг 2: Сгенерировать код Drift
```bash
dart run build_runner build --delete-conflicting-outputs
```
Это создаст файл `lib/database/app_database.g.dart`

### Шаг 3: Запустить приложение
```bash
flutter run
```

---

## 📁 Структура проекта

```
Bissultan_Kuandykov_Day29_Flutter/
├── lib/
│   ├── database/
│   │   ├── tables.dart           # Определения таблиц
│   │   ├── app_database.dart     # Database класс с CRUD
│   │   └── app_database.g.dart   # Сгенерированный код
│   └── main.dart                 # UI с демонстрацией
├── pubspec.yaml
└── README.md
```

---

## 🎯 Как тестировать

### Tab 1: Stream (Автообновление) ✅

1. Нажмите "+" и добавьте задачу "Купить молоко"
2. **Результат:** Задача появится **МГНОВЕННО** без перезагрузки
3. Отметьте чекбокс (выполнена)
4. **Результат:** UI обновится **АВТОМАТИЧЕСКИ** - текст зачеркнется
5. Нажмите "Удалить"
6. **Результат:** Задача исчезнет **МГНОВЕННО**

**Вывод:** UI обновляется БЕЗ setState, БЕЗ ручного обновления!

---

### Tab 2: Get (Ручное обновление) ⚠️

1. Нажмите "+" и добавьте задачу "Купить хлеб"
2. **Результат:** Задача **НЕ ПОЯВИТСЯ** в списке
3. Нажмите кнопку 🔄 (обновить) справа
4. **Результат:** Теперь задача видна
5. Отметьте чекбокс
6. **Результат:** Чекбокс изменился, но список **НЕ ОБНОВИЛСЯ**
7. Снова нажмите 🔄
8. **Результат:** Теперь изменение видно

**Вывод:** Нужно ВРУЧНУЮ обновлять после КАЖДОГО действия!

---

### Tab 3: Сортировка

#### По дате:
- Создайте несколько задач
- Они автоматически отсортируются: новые первые

#### По приоритету:
- Создайте задачи с разными приоритетами (P1-P5)
- Они автоматически отсортируются: высокий приоритет первым
- При одинаковом приоритете - по дате

**Вывод:** Сортировка работает автоматически (Stream)!

---

## 💡 Ключевые концепции

### 1. Foreign Key
```dart
IntColumn get tagId => integer()
  .nullable()
  .references(Tags, #id, onDelete: KeyAction.setNull)();
```
- При удалении тега → `tagId` становится `NULL`
- `nullable()` - тег опционален

### 2. Stream vs Get

| Характеристика | Stream (watch) | Get |
|---------------|----------------|-----|
| Обновление    | Автоматическое ✅ | Ручное ❌ |
| setState      | Не нужен ✅ | Обязателен ❌ |
| Код           | Меньше ✅ | Больше ❌ |
| Актуальность  | Всегда ✅ | Может устареть ❌ |

**Рекомендация:** Используйте Stream в 95% случаев!

### 3. Сортировка
```dart
Stream<List<Task>> watchTasksSortedByPriority() {
  return (select(tasks)
    ..orderBy([
      (t) => OrderingTerm(expression: t.priority, mode: OrderingMode.desc),
      (t) => OrderingTerm(expression: t.createdAt, mode: OrderingMode.desc),
    ]))
      .watch();
}
```
- Сначала по приоритету (высокий → низкий)
- Потом по дате (новые → старые)

### 4. JOIN запросы
```dart
Stream<List<TaskWithTag>> watchTasksWithTags() {
  final query = select(tasks).join([
    leftOuterJoin(tags, tags.id.equalsExp(tasks.tagId)),
  ]);
  
  return query.watch().map((rows) {
    return rows.map((row) {
      return TaskWithTag(
        task: row.readTable(tasks),
        tag: row.readTableOrNull(tags), // может быть null
      );
    }).toList();
  });
}
```
- `leftOuterJoin` - включает задачи БЕЗ тега
- Автоматически обновляется при изменении любой таблицы

---

## 🔑 CRUD операции

### Create
```dart
await database.createTask(
  title: 'Купить молоко',
  priority: 3,
);
```

### Read
```dart
// Stream - автообновление
Stream<List<Task>> watchAllTasks() {
  return select(tasks).watch();
}

// Get - одноразовый запрос
Future<List<Task>> getAllTasksOnce() {
  return select(tasks).get();
}
```

### Update
```dart
await database.updateTask(
  task.copyWith(
    title: 'Новое название',
    priority: 5,
    updatedAt: Value(DateTime.now()),
  ),
);
```

### Delete
```dart
await database.deleteTask(taskId);
```

---

## 📊 Демонстрация разницы

### Stream работает так:
```
1. UI подписывается на Stream
   ↓
2. Drift отслеживает изменения в таблице
   ↓
3. При ЛЮБОМ изменении (INSERT/UPDATE/DELETE)
   ↓
4. Drift автоматически эмитит новые данные в Stream
   ↓
5. StreamBuilder получает обновление
   ↓
6. UI автоматически перерисовывается
   ↓
✅ НЕ НУЖНО: setState(), ручное обновление
```

### Get работает так:
```
1. UI вызывает get()
   ↓
2. Получает данные
   ↓
3. Отображает их
   ↓
4. Данные изменились в БД
   ↓
❌ UI НЕ ЗНАЕТ об изменениях!
   ↓
5. Нужно ВРУЧНУЮ вызвать get() снова
   ↓
6. Обновить setState()
   ↓
⚠️ ЛЕГКО ЗАБЫТЬ → устаревшие данные
```

---

## 🎓 Что изучено

✅ Создание связанных таблиц (Foreign Key)  
✅ CRUD операции (Create, Read, Update, Delete)  
✅ Сортировка по нескольким полям  
✅ JOIN запросы для объединения таблиц  
✅ Stream (watch) для реактивного программирования  
✅ Сравнение Stream vs Get  
✅ Companion классы для типобезопасной вставки  
✅ copyWith для immutable обновлений  
✅ Offline-first архитектура  

---

## 🐛 Возможные ошибки

### Ошибка: "app_database.g.dart not found"
**Решение:**
```bash
dart run build_runner build --delete-conflicting-outputs
```

### Ошибка: "No such table: tasks"
**Решение:** Удалите приложение и переустановите

### Ошибка при импорте
**Решение:** Проверьте `part 'app_database.g.dart';` в app_database.dart

---

## 📞 Автор

**Bissultan Kuandykov**  
Day 29 - Flutter Course  
Тема: Локальная база данных (Drift + SQLite)

---

## 🌟 Особенности реализации

1. **Production-ready код**
   - Правильная архитектура (слои отделены)
   - Типобезопасность на этапе компиляции
   - Обработка null значений
   - Отслеживание updatedAt

2. **Наглядная демонстрация**
   - 3 вкладки для сравнения
   - Видно разницу Stream vs Get в реальном времени
   - Интерактивные примеры

3. **Полная функциональность**
   - CRUD для двух таблиц
   - JOIN запросы
   - Множественная сортировка
   - Фильтрация (можно легко добавить)

---

🎉 Готово! Полностью рабочее приложение с демонстрацией всех концепций Drift + SQLite
#   - B i s s u l t a n _ K u a n d y k o v _ D a y 2 9 _ F l u t t e r  
 